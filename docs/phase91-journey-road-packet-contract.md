# Phase 91 Pass 1C: Journey Road Packet Contract

The Journey Road packet is `journey_road_summary`.

Required rows:
- `$1M road`
- current mission
- current checkpoint
- next road marker

The packet is capped at four rows and is read-only.

