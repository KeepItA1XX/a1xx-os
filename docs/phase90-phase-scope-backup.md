# Phase 90 Pass 1A: Phase Scope + Backup

Phase 90 is scoped to `Account > Mission > Missions > Active`.

The pass uses the existing `mission-active-player-surface` slot and does not add a second Active card. Timestamped backups were created on A1XX ZDRIVE before editing.

Protected actions remain blocked: no app writes, mission completion writes, XP awards, notifications, workers, automations, restore execution, token export, or secret export.
